import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  root: {
    display: "flex",
    width: "100vw",
    height: "100vh",
    overflowX: "hidden",
    overflowY: "hidden",
    justifyContent: "center",
    alignItems: "center",
  },
}));
